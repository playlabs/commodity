configuration:
  projectName: commodity-test1
server:
  port: 8888

---

spring:
 profiles: development
 datasource:
  url: jdbc:mysql://localhost:3306/sakila
  username: root
  password: password
  driver-class-name: com.mysql.jdbc.Driver
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
server:
  port: 9014
logging:
  level:
    org.springframework.web: INFO
    org.springframework.controllers: INFO
    org.hibernate: INFO
---

spring:
 profiles: qa
 datasource:
  url: jdbc:mysql://localhost:3306/qa_sakila
  username: root
  password: password
  driver-class-name: com.mysql.jdbc.Driver
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
server:
  port: 9012
